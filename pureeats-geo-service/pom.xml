<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>com.pureeats</groupId>
		<artifactId>pureeats-parent</artifactId>
		<version>0.0.1-SNAPSHOT</version>
		<relativePath>../pom.xml</relativePath>
	</parent>

	<artifactId>pureeats-geo-service</artifactId>
	<packaging>jar</packaging>
	<name>pureeats-geo-service</name>
	<description>Distance calculation (Haversine/Euclidean/Google Distance Matrix) and other
		location-related utilities, shared by catalog-service and order-service. No JPA entities of
		its own - a plain-code module, not a runnable service, the same shape as domain.</description>

	<dependencies>
		<!--
			Only what the DistanceCalculator implementations actually need:
			spring-boot-autoconfigure for @ConditionalOnProperty-selected beans (DistanceCalculatorConfig),
			spring-web for RestClient (GoogleDistanceMatrixCalculator). No JPA, no web server starter -
			this module is a library other services depend on, not something deployed on its own.
		-->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-autoconfigure</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-api</artifactId>
		</dependency>

		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>

		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

</project>
